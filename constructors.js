function Dog(name, breed, weight) {
    // this = {}
    this.name = name,
    this.breed = breed,
    this.weight = weight

    // return this
    
}

var pluto = new Dog("Charlie", "Mixed", 38)
console.log(pluto.name)